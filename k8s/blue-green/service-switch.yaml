apiVersion: v1
kind: Service
metadata:
  name: inventario-service
spec:
  type: NodePort
  selector:
    app: inventario-app
    version: blue  
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000